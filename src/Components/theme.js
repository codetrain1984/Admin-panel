import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#007fcb',
      light: '#39b3ff',
    },
    secondary: {
      main: '#84ebff',
    },
    otherColor: {
      main: '#ff69b4',
    },
  },
})
